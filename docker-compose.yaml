services:
  streamlit:
    container_name: web-pmb-streamlit
    build: .
    command: streamlit run app/chat.py --server.port 4001 --server.address 0.0.0.0
    env_file:
      - .env
    ports:
      - "4001:4001"
    restart: always
    volumes:
      - .:/app
